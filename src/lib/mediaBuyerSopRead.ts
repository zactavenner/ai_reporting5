/**
 * Frontend view of the shared SOP read adapter. Pure re-export so the preview UI
 * and the prepared edge function read identical columns and produce identical
 * blockers.
 */
export * from '../../supabase/functions/_shared/mediaBuyerSopRead';
